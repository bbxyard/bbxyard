	// set the model and render the view template.
		Layout: "admin/layout.html", // 不要跟前端的layout混用
	//fmt.Println(id, data)
	// set the model and render the view template.
		Layout: "admin/layout.html", // 不要跟前端的layout混用
	//fmt.Printf("%v\n", info)
